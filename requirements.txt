# Runtime dependencies — pinned where API stability matters,
# ranges for packages shared across the transitive dependency graph.

# Core framework — ranges to avoid conflicts with mcp/anthropic transitive deps
pydantic>=2.7.2,<3
pydantic-settings>=2.5.2,<3

# CLI & rendering
click==8.1.7
rich==13.7.1

# HTTP client — range to satisfy both anthropic (>=0.23) and mcp (>=0.27.1)
httpx>=0.27.1,<1

# AWS
boto3==1.34.110

# LLM
anthropic==0.28.0

# MCP SDK
mcp==1.9.4

# Vector store & embeddings
chromadb>=0.5.23,<0.6.0
sentence-transformers==3.0.1

# Templating & config
jinja2==3.1.4
pyyaml==6.0.1
