[metadata]
name = security-pr-guardian
version = attr: security_pr_guardian.__version__

[options]
packages = find:
python_requires = >=3.11

[options.packages.find]
include =
    security_pr_guardian
    security_pr_guardian.*
