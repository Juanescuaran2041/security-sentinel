diff --git a/app/views/profile.py b/app/views/profile.py
--- a/app/views/profile.py
+++ b/app/views/profile.py
@@ -1,8 +1,28 @@
-from flask import render_template, request
+from flask import render_template, render_template_string, request, Markup
 
 
 def show_profile(user):
-    return render_template("profile.html", user=user)
+    # Vulnerable: render_template_string with user input
+    template = f"<h1>Welcome {user.name}</h1><p>{user.bio}</p>"
+    return render_template_string(template)
+
+
+def show_search_results(query: str, results: list):
+    """Search results — vulnerable via Markup with f-string."""
+    header = Markup(f"<h2>Results for: {query}</h2>")
+    return render_template("search.html", header=header, results=results)
+
+
+def render_comment(comment_text: str):
+    """Render a user comment — vulnerable via Jinja2 |safe filter."""
+    template = """
+    <div class="comment">
+        {{ comment_text | safe }}
+    </div>
+    """
+    return render_template_string(template, comment_text=comment_text)
+
+
+def admin_message(msg: str):
+    """Admin message — vulnerable via f-string with HTML tag."""
+    return f"<div class='alert'>{msg}</div>"
diff --git a/frontend/src/components/UserCard.jsx b/frontend/src/components/UserCard.jsx
--- a/frontend/src/components/UserCard.jsx
+++ b/frontend/src/components/UserCard.jsx
@@ -1,9 +1,20 @@
 import React from 'react';
 
-export function UserCard({ name, bio }) {
+export function UserCard({ name, bio, htmlContent }) {
   return (
     <div className="user-card">
       <h3>{name}</h3>
-      <p>{bio}</p>
+      {/* Vulnerable: dangerouslySetInnerHTML with user data */}
+      <div dangerouslySetInnerHTML={{ __html: htmlContent }} />
     </div>
   );
 }
+
+export function RawOutput({ data }) {
+  // Vulnerable: document.write via useEffect
+  React.useEffect(() => {
+    document.write(data);
+  }, [data]);
+  return null;
+}
diff --git a/frontend/src/utils/dom.js b/frontend/src/utils/dom.js
--- a/frontend/src/utils/dom.js
+++ b/frontend/src/utils/dom.js
@@ -1,4 +1,10 @@
 export function updateElement(id, content) {
-  document.getElementById(id).textContent = content;
+  // Vulnerable: innerHTML assignment with dynamic content
+  document.getElementById(id).innerHTML = content;
+}
+
+export function renderNotification(message) {
+  // Vulnerable: document.write
+  document.write(`<div class="notification">${message}</div>`);
 }
